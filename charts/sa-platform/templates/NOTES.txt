sa-platform installed in namespace {{ .Values.namespace.name }}.

{{- if .Values.ingress.enabled }}
Ingress host:
  http://{{ .Values.ingress.host }}
{{- else }}
Gateway service:
  kubectl get svc api-gateway -n {{ .Values.namespace.name }}
{{- end }}

Next checks:
  helm history {{ .Release.Name }} -n {{ .Values.namespace.name }}
  kubectl get pods -n {{ .Values.namespace.name }}
  kubectl get svc -n {{ .Values.namespace.name }}
  kubectl get hpa -n {{ .Values.namespace.name }}
